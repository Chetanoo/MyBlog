query Post($limit: Int!, $cursor: String) {
    posts(limit: $limit, cursor: $cursor) {
        posts {
            createdAt
            updatedAt
            id
            title
            text
            textSnippet
            rating
            creatorId
        }
        hasMore
    }
}
